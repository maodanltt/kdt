<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.tywh.kdt.report.mapper.BookMapper">
    <select id="queryBookList" parameterType="string" resultType="book">
        select sxh,shum,bmmc,tsfljc,zbh,gjdj
        from fxsgkview order by shum desc,sxh desc
<!--        <where>-->
<!--            shum like '%' + #{shum} + '%'-->
<!--        </where>-->
<!--        order by sxh desc-->
    </select>
</mapper>